{
  "name": "mixch-archive-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "npm test && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "wrangler": "4.129.1" }
}
